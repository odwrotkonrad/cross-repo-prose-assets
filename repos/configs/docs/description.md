Dotfiles extended into root OS space. One `root/` tree loads onto the host: symlinked by default, copied where marked `.ontoHost.cp`, rendered from `*.ontoHost.tpl`. Repo docs, the license, and the AI prose payloads render from the prose repo at a pinned version. Only the local `.env` seed renders from a repo-local `*.ontoRepo.tpl`.
