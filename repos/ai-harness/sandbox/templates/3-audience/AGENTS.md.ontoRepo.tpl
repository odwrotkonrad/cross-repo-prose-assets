# sandbox

Local claude session sandbox: kind cluster plus per-session pods running the config-baked sandbox image this repo builds and publishes.

@assets/docs-agents/purpose.md

This repo's behavior specs live in the spec repo (`cross-repo/prose/spec`): `repos/ai-harness/sandbox/spec/` (`.story.md` user stories by default, `.scenarios.md` Gherkin only for automation-bound behavior, status in each title, vetting dirs apply, edit specs before implementation).

@assets/data/makefile.agents.md

## Directory Tree

@assets/data/repo-structure.md
